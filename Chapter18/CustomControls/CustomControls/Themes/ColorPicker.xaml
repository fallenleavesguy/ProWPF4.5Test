<ResourceDictionary
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:CustomControls">
    <Style TargetType="{x:Type local:ColorPicker}">
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type local:ColorPicker}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <Slider
                            Name="PART_RedSlider"
                            Margin="{TemplateBinding Padding}"
                            Maximum="255"
                            Minimum="0" />
                        <Slider
                            Name="PART_GreenSlider"
                            Grid.Row="1"
                            Margin="{TemplateBinding Padding}"
                            Maximum="255"
                            Minimum="0" />
                        <Slider
                            Name="PART_BlueSlider"
                            Grid.Row="2"
                            Margin="{TemplateBinding Padding}"
                            Maximum="255"
                            Minimum="0" />
                        <!--<Slider
                            Margin="{TemplateBinding Padding}"
                            Maximum="255"
                            Minimum="0"
                            Value="{Binding Path=Red, RelativeSource={RelativeSource TemplatedParent}}" />
                        <Slider
                            Grid.Row="1"
                            Margin="{TemplateBinding Padding}"
                            Maximum="255"
                            Minimum="0"
                            Value="{Binding Path=Red, RelativeSource={RelativeSource TemplatedParent}}" />
                        <Slider
                            Grid.Row="2"
                            Margin="{TemplateBinding Padding}"
                            Maximum="255"
                            Minimum="0"
                            Value="{Binding Path=Red, RelativeSource={RelativeSource TemplatedParent}}" />-->
                        <Rectangle
                            Grid.RowSpan="3"
                            Grid.Column="1"
                            Width="50"
                            Margin="{TemplateBinding Padding}"
                            Stroke="Black"
                            StrokeThickness="1">
                            <Rectangle.Fill>
                                <SolidColorBrush x:Name="PART_PreviewBrush" Color="{Binding Path=Color, RelativeSource={RelativeSource TemplatedParent}}" />
                            </Rectangle.Fill>
                        </Rectangle>
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>
</ResourceDictionary>